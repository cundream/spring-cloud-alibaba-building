server:
  port: 18000
spring:
  application:
    name: spring-cloud-building

  cloud:
    nacos:
      config:
        file-extension: yaml # 配置文件格式


  profiles:
    active: dev

management:
  endpoint:
    web:
      exposure:
        include:'*'
