server:
  port: 18000
spring:
  application:
    name: spring-cloud-building

  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848 #nacos中心地址
        file-extension: yaml # 配置文件格式


  profiles:
    active: dev

management:
  endpoint:
    web:
      exposure:
        include:'*'
