server:
  port: 18000
spring:
  application:
    name: spring-cloud-building

  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848

management:
  endpoint:
    web:
      exposure:
        include:'*'
